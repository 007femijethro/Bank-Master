DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
SESSION_SECRET=replace-with-at-least-32-random-bytes
# Optional separate key; when omitted, SESSION_SECRET is domain-separated for field encryption.
DATA_ENCRYPTION_KEY=
COOKIE_SECURE=true
PORT=5000
